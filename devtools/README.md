# devtools

개발·디버깅용 스크립트 모음 (배포/일일 실행과 무관).

| 폴더 | 내용 |
|------|------|
| `debug/` | 소스별 RSS/API 실험 스크립트 |
| `tests/` | `test_mastodon.js`, `test_rss.js` 등 |
| `archive/twitter/` | X(Twitter) Playwright 수집 (현재 비활성화) |

루트에서 실행 예:

```bash
node devtools/debug/debug_reddit.js
```
